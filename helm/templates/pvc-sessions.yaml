apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "kraclaw.fullname" . }}-sessions
  namespace: {{ .Release.Namespace }}
  labels: {{ include "kraclaw.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storage.sessions.size }}
  storageClassName: {{ .Values.storage.class }}
