apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "kraclaw.fullname" . }}-groups
  namespace: {{ .Release.Namespace }}
  labels: {{ include "kraclaw.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storage.groups.size }}
  storageClassName: {{ .Values.storage.class }}
