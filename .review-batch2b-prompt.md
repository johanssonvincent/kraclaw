Repo: /opt/data/workspace/kraclaw (Go, branch fix/review-high-severity — commit on it, do NOT create branches). Follow CLAUDE.md conventions: slog, fmt.Errorf("%w"), table-driven tests.

TDD IS MANDATORY: failing test first, watch it fail, implement, green. Note which tests were watched failing.

EDIT PROTOCOL (mandatory):
1. Files under ~100 lines: write_file with COMPLETE content.
2. Larger files: patch with SHORTEST unique anchor (1-3 lines). On failure, re-read and copy anchor verbatim; never retry the same old_string.
3. Read changed regions back before reporting done.
4. Verification gate at the end (must be all green):
   export PATH=/opt/data/.local/go/bin:/opt/data/.local/bin:$PATH
   go build ./... && go vet ./... && gofumpt -w internal/ cmd/ pkg/
   go test -short -race -count=1 ./internal/orchestrator/ ./internal/server/ ./internal/channel/... ./internal/sandbox/
   CI-blockers: errorlint (%w not %v), nlreturn (blank line before EVERY return), gofumpt.

SCOPE: ONLY these three fixes. Touch nothing else.

FIX 1 — Telegram self-echo filter is dead code (internal/channel/telegram/telegram.go).
The struct has a botID field (line ~40) used at line ~83 (msg.From.ID == t.botID) but it is never assigned, so the filter never matches and the bot can process its own outgoing messages (echo loop).
Fix: after the bot connects / GetMe succeeds in Connect, set t.botID from the bot's own ID. Inspect how tgbot (github.com/go-telegram/bot or similar — check go.mod and imports) exposes the bot's ID; use GetMe.
Test first in the telegram package: simulate an update whose From.ID equals the bot ID and assert the handler drops it; a different sender passes. If Connect's GetMe is hard to fake, factor so botID is injectable (e.g. set on the struct in the test).

FIX 2 — StreamSandboxOutput ignores broker error channel (internal/server/api_services.go ~line 716).
`ch, _, err := s.ipc.SubscribeOutput(...)` discards errCh; when the consume goroutine dies the stream silently returns nil and the client thinks output ended cleanly.
Fix: capture errCh and in the stream loop's select, also receive from errCh; on error, log and return status.Errorf(codes.Internal, "ipc output stream: %w", err) (or Unknown). Check ipc.SubscribeOutput's contract in internal/ipc to confirm errCh semantics (does it also close ch?).
Test first in api_services_test.go (existing tests use embedded NATS): force a broker error (e.g. close the underlying NATS server or an invalid subscription) and assert the stream returns an error instead of silently ending. If forcing the error is impractical, at least test that a normal stream still works and add a focused unit test for a helper that selects on both channels.

FIX 3 — sandbox watcher drops Deleted events when channel is full (internal/sandbox/watcher.go ~line 102).
Events are dropped with a warning when the channel is full; a dropped Deleted event can strand sandbox state until the next poll.
Fix: never drop Deleted events — when the channel is full, block (bounded by the watcher ctx) to deliver them; non-delete events may still be dropped. Keep the goroutine cancellable via ctx.
Test first in watcher_test.go (uses fake kubernetes clientset): fill the channel, emit a Deleted event, assert the consumer still receives it.

Report: files changed, tests watched failing, verification-gate output.
