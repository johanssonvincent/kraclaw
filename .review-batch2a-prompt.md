Repo: /opt/data/workspace/kraclaw (Go, branch fix/review-high-severity already checked out — commit on it, do NOT create branches). Follow CLAUDE.md conventions: slog, fmt.Errorf("%w"), table-driven tests.

TDD IS MANDATORY: write the failing test FIRST, run it to see it fail with the right assertion, then implement, then re-run until green. Note which tests were watched failing in your summary.

EDIT PROTOCOL (mandatory):
1. Files under ~100 lines: use write_file with COMPLETE content. Never patch small files.
2. Larger files: use patch with the SHORTEST unique anchor (1-3 lines). If a patch fails, do NOT retry the same old_string — re-read the file and copy the anchor verbatim.
3. After ONE failed patch attempt, re-read before the next attempt.
4. Read changed regions back before reporting done.
5. Verification gate at the end (must be all green):
   export PATH=/opt/data/.local/go/bin:/opt/data/.local/bin:$PATH
   go build ./... && go vet ./... && gofumpt -w internal/ cmd/ pkg/
   go test -short -race -count=1 ./internal/scheduler/ ./internal/orchestrator/ ./internal/store/ ./internal/server/
   CI-blockers: errorlint (%w not %v), nlreturn (blank line before EVERY return), gofumpt.

SCOPE: ONLY these three fixes. Touch nothing else.

FIX 1 — IPCTaskUpdate skips task.Validate() (internal/orchestrator/orchestrator.go ~line 1823).
IPCTaskCreate validates the task before storing; IPCTaskUpdate does not. An agent can write an invalid schedule/prompt that only surfaces later as a paused task.
Fix: in IPCTaskUpdate, run the same validation IPCTaskCreate does before calling store.UpdateTask, and return an appropriate error to the agent on failure.
Test first in orchestrator_test.go: updating a task with an invalid schedule type must be rejected and the store must not be called.

FIX 2 — UpdateTask maps every store error to NotFound (internal/server/api_services.go ~line 447).
The handler calls GetTask first and returns codes.NotFound for ANY error, including internal DB failures.
Fix: distinguish errors.Is / store sentinel errors: keep codes.NotFound only for genuine not-found (check how other handlers in the same file distinguish this — follow the existing pattern), map other errors to codes.Internal.
Test first in api_auth_test.go or api_services_test.go (whichever file holds UpdateTask tests): a store returning a non-not-found error must yield codes.Internal.

FIX 3 — RegisterGroup fabricates AddedAt (internal/server/api_services.go ~line 317).
The response uses time.Now() instead of the persisted group.AddedAt. Check what UpsertGroup returns; if it returns the stored group, use its AddedAt. If UpsertGroup doesn't return the group, adapt minimally (e.g. have the handler use the value the store provides).
Test: RegisterGroup response must echo the store's AddedAt, not a freshly generated one.

Report: files changed, tests watched failing, verification-gate output.
